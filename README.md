### **AC 1: Add Project Successfully**
---
| Test Case ID | Title | Case Type | Pre-conditions | Test Steps | Expected Results |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TC_PROJ_001** | **Happy Path - Create Project with Required Fields Only** | **Normal** | User is logged in with permissions to create a project and is on the Projects page. | 1. Click the "Create Project" button. <br> 2. In the "General" tab, fill in a unique **Project name** (e.g., "WebApp Scanner"). <br> 3. Select a value for **Collection filter** (e.g., "Aggregate with direct target"). <br> 4. Click the "Create" button. | 1. A success notification "Project created" appears. <br> 2. The "Create Project" pop-up closes. <br> 3. The new project "WebApp Scanner" appears at the top of the project list. |
| **TC_PROJ_002** | **Happy Path - Create Project with All Fields** | **Normal** | User is logged in with permissions and is on the Projects page. | 1. Click "Create Project". <br> 2. Fill in all fields in the **General** tab with valid data. <br> 3. Navigate to the **Identity** tab. <br> 4. Fill in all fields in the **Identity** tab with correctly formatted data. <br> 5. Click "Create". | 1. A success notification "Project created" appears. <br> 2. The pop-up closes. <br> 3. The new project appears at the top of the list with all its data saved correctly. |
| **TC_PROJ_003** | **Cancel Project Creation** | **Normal** | User is logged in and on the Projects page. | 1. Click "Create Project". <br> 2. Fill in some data (e.g., Project name). <br> 3. Click the "Cancel" button. | 1. The "Create Project" pop-up closes. <br> 2. No new project is added to the project list. |
| **TC_PROJ_004** | **UI/UX - Verify Tooltips** | **Normal** | User is logged in and the "Create Project" pop-up is open. | 1. In the "General" tab, hover the mouse over the question mark icon `?` next to each field. <br> 2. Go to the "Identity" tab and repeat for each field. | 1. The correct tooltip text appears for each field as specified in the ACs. |
| **TC_PROJ_005** | **UI/UX - Verify Dynamic Tag Field Appears** | **Normal** | User is logged in and the "Create Project" pop-up is open. | 1. In the "General" tab, click the **Collection filter** dropdown. <br> 2. Select "Aggregate with direct target with tag". | 1. A new text box for adding tags appears below the dropdown. |
| **TC_PROJ_006** | **UI/UX - Default Tab on Popup** | **Normal** | User is logged in and on the Projects page. | 1. Click the "Create Project" button. | 1. The "Create Project" pop-up appears. <br> 2. The **General** tab is active by default. |
| **TC_PROJ_007** | **UI/UX - Project Name Live Search** | **Normal** | A project named "DependencyTrack" already exists. The "Create Project" pop-up is open. | 1. In the "Project name" field, start typing "Depend". | 1. As the user types, a list of existing projects containing the keyword appears below the text box, including "DependencyTrack". |
| **TC_PROJ_008**| **UI/UX - Identity Field Placeholders**| **Normal**| User is logged in and the "Create Project" pop-up is open. | 1. Navigate to the **Identity** tab. | 1. Each text box displays the correct placeholder text as specified in the ACs (e.g., "Package URL (PURL)"). |

### **AC 2: Validation When Creating a Project**
---
| Test Case ID | Title | Case Type | Pre-conditions | Test Steps | Expected Results |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TC_PROJ_009** | **Validation - Empty Required Fields** | **Abnormal** | User is logged in and the "Create Project" pop-up is open. | 1. Leave the **Project name** field blank. <br> 2. Click the "Create" button. | 1. An error message "Please input this field" appears below the Project name field. <br> 2. The pop-up remains open and the project is not created. |
| **TC_PROJ_010** | **Validation - Duplicate Project Name + Suffix** | **Abnormal** | A project named "API-Gateway" with Suffix "v2" already exists. | 1. Click "Create Project". <br> 2. Enter "API-Gateway" as the **Project name**. <br> 3. Enter "v2" as the **Suffix**. <br> 4. Click "Create". | 1. An error message "Existing project. Please adjust the project name and suffix." is displayed. <br> 2. The project is not created. |
| **TC_PROJ_011** | **Validation - Duplicate Project Name with Empty Suffix** | **Abnormal** | A project named "Dashboard" with an empty Suffix already exists. | 1. Click "Create Project". <br> 2. Enter "Dashboard" as the **Project name**. <br> 3. Leave the **Suffix** field empty. <br> 4. Click "Create". | 1. An error message "Existing project. Please adjust the project name and suffix." is displayed. |
| **TC_PROJ_012** | **Validation - Invalid Special Characters in Project Name**| **Abnormal** | User is logged in and the "Create Project" pop-up is open. | 1. Attempt to type a name with special characters into the **Project name** field (e.g., "Project@#$"). | 1. The system prevents the input of special characters (`@`, `#`, `$`). Only letters and numbers can be entered. |
| **TC_PROJ_013** | **Validation - Incorrect Format in Identity Fields** | **Abnormal** | User is logged in and the "Create Project" pop-up is open on the Identity tab. | 1. Enter text that does not match the required format into the **Package URL (PURL)** field (e.g., "invalid-purl"). <br> 2. Click "Create". | 1. An error message "Please fill in value in correct format." appears below the PURL field. <br> 2. The project is not created. |
| **TC_PROJ_014** | **Validation - Multiple Incorrect Format Errors** | **Abnormal** | User is logged in and the "Create Project" pop-up is open on the Identity tab. | 1. Enter incorrectly formatted text into the **PURL** field. <br> 2. Enter incorrectly formatted text into the **CPE** field. <br> 3. Click "Create". | 1. An error message "Please fill in value in correct format." appears under **both** the PURL and CPE fields. |
| **TC_PROJ_015** | **Validation - Tag Input Limit** | **Abnormal** | User is logged in and the "Create Project" pop-up is open. | 1. In the **General** tab, add 5 tags to the "Tag" field. <br> 2. Attempt to add a 6th tag. | 1. The system prevents the user from adding the 6th tag. |
| **TC_PROJ_016** | **Boundary - Extremely Long Input in Optional Field** | **Abnormal** | User is logged in and the "Create Project" pop-up is open. | 1. Enter an extremely long string (e.g., 10,000 characters) into the optional **Suffix** field. <br> 2. Fill in all other required fields correctly. <br> 3. Click "Create". | 1. The system handles the long input gracefully, either by: <br> &nbsp;&nbsp; a) Displaying a validation error that the field is too long, OR <br> &nbsp;&nbsp; b) Successfully creating the project, truncating the data if necessary. <br> 2. The application UI and backend do not crash. |
| **TC_PROJ_017** | **UI/UX - Cancel After Validation Error** | **Abnormal** | User is logged in and on the Projects page. | 1. Click "Create Project". <br> 2. Leave **Project name** blank and click "Create" to trigger a validation error. <br> 3. Click the "Cancel" button. <br> 4. Click "Create Project" again. | 1. The pop-up closes correctly on "Cancel". <br> 2. When the pop-up is re-opened, the form is in its default, clean state with no lingering error messages from the previous attempt. |

### **AC 3: View Project List**
---
| Test Case ID | Title | Case Type | Pre-conditions | Test Steps | Expected Results |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **TC_PROJ_018** | **View Project List - With Data** | **Normal** | At least one project exists in the system. | 1. Navigate to the Projects page (`Home > Projects`). | 1. A table displays the list of projects. <br> 2. The most recently created project is at the top. <br> 3. Columns (`Project name`, `Suffix`, `Last BOM Import`, etc.) are displayed correctly. <br> 4. The project counter shows the correct total number of projects. |
| **TC_PROJ_019** | **View Project List - No Data** | **Normal** | No projects have been created in the system. | 1. Navigate to the Projects page. | 1. The table area is empty and displays a "No data" message. |
| **TC_PROJ_020** | **UI/UX - Default Sort Order** | **Normal**| Multiple projects with different creation dates exist. | 1. Navigate to the Projects page. | 1. The project list is sorted by the **Last updated date** column in descending order (newest on top) by default. |
| **TC_PROJ_021** | **UI/UX - Search with Partial Match** | **Normal** | Multiple projects exist, including one named "Kernel-Linux-Prod". | 1. On the Projects page, type "Linux" into the search text box. <br> 2. Press Enter. | 1. The project list is filtered to show only projects containing "Linux" in their name, including "Kernel-Linux-Prod". |
| **TC_PROJ_022** | **UI/UX - Search with No Results** | **Normal** | The project list is displayed. | 1. Type a non-existent project name (e.g., "XYZ123ABC") into the search box and press Enter. | 1. The table becomes empty and a "No data" message is displayed. |
| **TC_PROJ_023** | **UI/UX - Pagination** | **Normal** | More than 25 projects exist. | 1. Navigate to the Projects page. | 1. The list displays 25 projects. <br> 2. Pagination controls are visible at the bottom of the table, allowing navigation to the next page. |
| **TC_PROJ_024** | **UI/UX - Column Visibility** | **Normal** | The project list is displayed. | 1. Click on the column selection menu. <br> 2. Uncheck the checkbox for the "Suffix" column. | 1. The "Suffix" column is immediately hidden from the table view. |
| **TC_PROJ_025**| **UI/UX - Breadcrumb Navigation** | **Normal** | The project list is displayed. | 1. Look at the top of the page. | 1. The breadcrumb `Home > Project` is correctly displayed. |
| **TC_PROJ_026**| **UI/UX - Clickable Project Name** | **Normal** | At least one project exists. | 1. In the project list, click on the name of any project. | 1. The user is navigated to the detailed view of that specific project. |
| **TC_PROJ_027**| **UI/UX - Reload Button Clears Search** | **Normal** | The project list has been filtered using the search box. | 1. Click the "Reload" button. | 1. The search filter is cleared. <br> 2. The project list refreshes and displays the full, unfiltered list of projects. |
